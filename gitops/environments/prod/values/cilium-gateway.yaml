# Prod Cilium Gateway for riverrise.net (HTTP listener; TLS later).
gateway:
  name: cilium
  gatewayClassName: cilium
  listeners:
    web:
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: All

lb:
  enabled: false
