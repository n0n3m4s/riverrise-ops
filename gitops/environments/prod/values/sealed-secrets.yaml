# Prod overrides for Sealed Secrets controller.
sealed-secrets:
  keyrenewperiod: "0"
  secretName: sealed-secrets-key
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi
