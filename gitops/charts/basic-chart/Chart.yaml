apiVersion: v2
name: basic-chart
description: Shared application chart for riverrise services (Deployment, Service, Ingress, HTTPRoute, HPA with behavior).
type: application
version: 0.1.0
appVersion: "1.0.0"
